# 班員用操作手順一覧

日常の検品操作は、基本的にWeb画面上だけで行います。

## 1. Webアプリを起動する

PowerShellでプロジェクトを開き、次を実行します。

```powershell
npm run dev
```

ブラウザーで [http://localhost:3000](http://localhost:3000) を開きます。`npm run bridge` の起動は不要です。

## 2. ログインする

1. メールアドレスとパスワードを入力します。
2. 「ログイン」を押します。

開発用アカウント：`staff@example.com` / `staff1234`

## 3. USB端末を新規登録する

1. 画面上部の「新規検品」を開きます。
2. スマートフォンのロックを解除し、USBで検品PCに接続します。
3. Androidは「USBデバッグを許可」iPhoneは「このコンピュータを信頼」を承認します。
4. 「USB接続した端末情報を取得」を押します。
5. 表示されたモデル名・容量・IMEI・バッテリー最大容量・充電回数を確認します。空欄は手入力します。
6. 必要に応じて正面・背面・画面の写真を選びます。
7. 「検品を登録」を押します。

## 4. 登録後の検品を進める

「検品履歴」から対象端末の「詳細」を開き、必要な操作を行います。

- 写真を追加：「画像を追加」でアングルと写真を選び、「追加」を押す
- IMEI確認：「IMEI・通信制限を照会」を押す
- 傷検出・グレード判定：写真を1枚以上登録し、「画像を解析・グレード判定」を押す
- 進捗更新：「未着手」「検品中」「完了」のいずれかを押す
- メモ保存：内容を入力し、「メモを保存」を押す

## 5. 過去の検品を探す

1. 「検品履歴」を開きます。
2. モデル名・IMEI、グレード、状態、日付を指定します。
3. 「検索」を押します。条件を戻すときは「解除」を押します。

## 6. 集計を確認する

「ダッシュボード」を開き、検品件数・グレード分布・平均処理時間を確認します。

## 7. 参考買取価格を更新する（管理担当）

PowerShellで次を実行します。通常は24時間以内の取得結果が再利用されます。

```powershell
npm run scrape:prices
```

この価格は現在DBに保存される参考値で、Web画面にはまだ表示されません。

## うまくいかないとき

- USB端末が見つからない：ケーブル、ロック解除、端末側の許可を確認して再取得する
- 一部の項目が空欄：端末の制限で取得できない場合があるため、手入力する
- 画像解析を押せない：先に画像を1枚以上登録する
- 困ったとき：画面のエラー文を管理担当に伝える

## 8. 終了する

1. 画面右上の「ログアウト」を押します。
2. Webアプリも終了する場合は、PowerShellで `Ctrl + C` を押します。
